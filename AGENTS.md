# Repository Instructions

This repository currently contains only a simple README. If you add automated tests, run `pytest` from the repository root.

Avoid adding large binary files. If a file must exceed 1MB, consider using Git LFS.
